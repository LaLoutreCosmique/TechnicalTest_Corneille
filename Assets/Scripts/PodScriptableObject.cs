using UnityEngine;

[CreateAssetMenu(fileName = "PodData", menuName = "PodObjectData")]
public class PodScriptableObject : ScriptableObject
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
